# Getting the ip-range.json
```
wget https://ip-ranges.amazonaws.com/ip-ranges.json
```

# Running tests
There is a single spec file, which can be used with `mocha` and a npm script to call run it:
```
npm test
```
